# nis-mc
Standard Monte Carlo wastes compute sampling empty phase space.
VEGAS adapts — but assumes independence. When QED radiation creates diagonal correlations, it fails catastrophically.
nis-mc trains a normalizing flow to perfectly trace the geometry of the matrix element,
reducing variance by orders of magnitude. No assumptions. No grid. Just learned geometry.
